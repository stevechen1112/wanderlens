# 測試環境

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:wanderlens_test}?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Taipei&allowPublicKeyRetrieval=true&useSSL=false
    username: ${MYSQL_USER:wanderlens_test}
    password: ${MYSQL_PASSWORD:test123}
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      database: 1

logging:
  level:
    com.wanderlens.api: DEBUG