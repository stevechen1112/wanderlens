name: CI - wanderlens-admin

on:
  pull_request:
    paths:
      - 'wanderlens-admin/**'
  push:
    branches: [main]
    paths:
      - 'wanderlens-admin/**'

jobs:
  build-and-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: wanderlens-admin/package-lock.json

      - name: Install dependencies
        working-directory: wanderlens-admin
        run: npm ci

      - name: Lint
        working-directory: wanderlens-admin
        run: npm run lint

      - name: Build
        working-directory: wanderlens-admin
        run: npm run build