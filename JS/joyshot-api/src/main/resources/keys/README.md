# Google OAuth 金鑰（本地參考用）

GitHub 僅保留 `secret_*.json.example`。若要在本機執行舊 joyshot-api，請自行複製：

```bash
cp secret_dev.json.example secret_dev.json
# 並填入已撤銷/輪替後的新金鑰，勿使用舊版外洩憑證
```

`secret_test.json`、`secret_prod.json` 同理。
